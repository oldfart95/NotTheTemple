FROM node:22-alpine

RUN corepack enable

WORKDIR /app

COPY package.json pnpm-workspace.yaml tsconfig.base.json ./
COPY apps ./apps
COPY packages ./packages
COPY services ./services

RUN pnpm install --no-frozen-lockfile

EXPOSE 4000

CMD ["pnpm", "--filter", "@market-tracker/api", "dev"]
